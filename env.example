# Desarrollo local
DATABASE_URL=sqlite:///camp_checklist.db
SECRET_KEY=change-me-in-production
FLASK_ENV=development

# Render (producción)
# DATABASE_URL se inyecta desde la base PostgreSQL kpi-db en render.yaml
